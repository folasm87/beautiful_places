<%= form_for(@loc_image) do |f| %>
  <% if @loc_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loc_image.errors.count, "error") %> prohibited this loc_image from being saved:</h2>

      <ul>
      <% @loc_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_location %><br>
    <%= f.text_field :image_location %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
